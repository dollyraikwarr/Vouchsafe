{
  "name": "vouchsafe",
  "version": "1.0.0",
  "description": "Vouchsafe - On-chain work escrow payment protocol on Stellar",
  "type": "module",
  "scripts": {
    "build": "echo 'Static build ready'",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@creit.tech/stellar-wallets-kit": "^1.1.2",
    "@stellar/freighter-api": "^2.0.0",
    "@stellar/stellar-sdk": "^14.0.0"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
